@model List<universidad1.Models.Alumno>

@{
    ViewData["Title"] = "Lista de Alumnos";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center">
        <h2>Alumnos Inscritos</h2>
        <a asp-action="Create" class="btn btn-success">Registrar Nuevo</a>
    </div>
    <hr />

    <table class="table table-striped table-hover">
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>Matrícula</th>
                <th>Nombre Completo</th>
                <th>Correo</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @if (Model == null || !Model.Any())
            {
                <tr>
                    <td colspan="4" class="text-center">No hay alumnos registrados.</td>
                </tr>
            }
            else
            {
                @foreach (var alumno in Model)
                {
                    <tr>
                        <td>@alumno.Id</td>
                        <td>@alumno.Matricula</td>
                        <td>@alumno.NombreCompleto</td>
                        <td>@alumno.Correo</td>
                        <td>
                           
                            <a asp-action="Edit" asp-route-id="@alumno.Id" class="btn btn-warning btn-sm">Editar</a>
                            <a asp-action="Delete" asp-route-id="@alumno.Id" class="btn btn-danger btn-sm">Eliminar</a>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>